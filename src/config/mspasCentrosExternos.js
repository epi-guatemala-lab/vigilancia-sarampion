/**
 * Centros Externos para el formulario MSPAS
 * Reutiliza las unidades médicas IGSS existentes, ya que los centros externos
 * en el formulario de vigilancia son las unidades IGSS que reportan.
 */
import { unidadesMedicasNombres } from './unidadesMedicas.js'

export const centrosExternosOptions = unidadesMedicasNombres
